/* eslint-disable no-new */
import test from 'ava';
import SelectLanguage from '../../src/menu/select-language';

test('should be stubbed for now', t => {
  t.is(new SelectLanguage().foo(), 'bar');
});
